"""结构专家和定位器共享的范围合同，不按题号推断范围。"""

STRUCTURE_SCOPE_CONTRACT = """结构范围与坐标合同：
1. 识别上下文是目标表格的完整页面及相关续页；输出范围只由题目明确要求决定。
2. 沿用本项目提交协议：row_count/col_count 表示完整目标表格的逻辑行列数；
   cells 仅包含题目要求的真实单元格，row/col 相对完整目标表格从 0 开始。
3. 全页图用于确定表格范围、完整行列和跨页关系；高清局部图仅帮助读清文本与边界。
   裁剪图不是新表格，不能在每个裁剪区域重新从第 0 行/列编号。
4. 若只要求表头，不得因为看到了数据行就把数据行也加入 cells；
   若要求第一列，不能只读取裁剪图里第一列的起始片段就当作完整第一列。
5. 多页和多个区域可能覆盖同一单元格，不因图像重复展示而重复输出。
   只有页面支持同一表格延续时才合并，不把页面上的其他表格拼入目标表格。
6. rowspan/colspan 必须依据可见合并边界；同一覆盖位置不能再输出另一个真实单元格。
   不通过删掉文字、随意缩短跨度或忽略冲突来凑出合法结构。
7. 横置表格先按文字正向理解逻辑行列；局部图可能已旋转校正，不能把纸面横纵方向
   当作逻辑行列。先识别最底层列边界，再确定父表头覆盖哪些子列；父子层级不等于跨行。
"""


def structure_recovery_content(
    full_content: list[dict[str, object]],
    region_content: list[dict[str, object]],
) -> list[dict[str, object]]:
    """保留全页上下文并区分后附高清区域；不修改调用者列表。"""
    return [
        {"type": "text", "text": "以下为原始完整页面，用于确定目标表格范围与全局坐标。"},
        *full_content,
        {"type": "text", "text": "以下为上述页面的高清局部图，仅补充细节，不重新定义行列坐标。"},
        *region_content,
    ]
